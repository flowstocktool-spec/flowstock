
# Database Configuration
# This will be automatically set by Replit when you create a PostgreSQL database
DATABASE_URL=postgresql://username:password@host:port/database

# Server Configuration
PORT=5000
NODE_ENV=development

# Optional: Email Configuration (set in application settings)
# These are configured through the web interface, not environment variables
# GMAIL_USER=your-email@gmail.com
# GMAIL_APP_PASSWORD=your-app-password
